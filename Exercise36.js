function canPlay() {
    var sport = " Football";
    
    if (true) {
    var personName = "Cosimo";
    }
    
    console.log(personName + sport);
    }
    
    canPlay();

    // We can use var instead of let while defining our variables.