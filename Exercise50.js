const names = ["Luca", "Marco", "Vittorio", "Giovanni"];

names.pop(); // Remove the element Giovanni.
names.push("Pippo"); // Add a new element called Pippo at the end of the array.
names.unshift("Giovanni") // Add the element Giovanni to the beginning of the array.

console.log(names);